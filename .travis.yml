matrix:
    include:
        - os: linux
        - os: osx

addons:
    apt:
        packages:
        - ed
        - qemu-system
        - openbios-ppc

git:
    depth: 10
language: c
script:
    - make PREFIX=/tmp/acki

