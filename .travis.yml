matrix:
    include:
        - os: linux
        - os: osx

addons:
    apt:
        packages:
        - ed
        - openbios-ppc
        - qemu-system-i386

git:
    depth: 10
language: c
script:
    - make PREFIX=/tmp/acki

